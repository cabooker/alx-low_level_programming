#include <stdio.h>

/**
*main - Prints all single digits of base 10 starting from 0
*followed by new line
*Return: Always 0
*/
int main(void)
{
int ch;
for (ch = 48; ch <= 57; n++)
{
putchar(ch);
}

putchar(10);

return (0); }
