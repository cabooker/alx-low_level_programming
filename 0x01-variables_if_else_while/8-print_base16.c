#include <stdio.h>

/**
*main - Prints all numbers of base 16 in lowercase
*
*Return: always 0 
*/
int main(void)
{
char ch;
int n
for (n = 48; n <= 57; n++)
{
putchar(n);
}
for (ch = 'a'; ch <= 'f'; n++)
{
putchar(ch);
}

putchar(10);

return (0);
} 